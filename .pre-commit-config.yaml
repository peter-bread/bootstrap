---
# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks

repos:
  - repo: local
    hooks:
      - id: update-checksum
        name: Update bootstrap.sh checksum
        entry: bash hooks/update-checksum.sh
        language: system
        stages:
          - pre-commit
        verbose: true
